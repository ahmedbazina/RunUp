import 'package:flutter/material.dart';
import 'package:runup/authenticate/auth.dart';
import 'package:runup/screens/home.dart';

class Root extends StatelessWidget
{
  @override
  Widget build(BuildContext context)
  {
    return Authenticate();
  }
}
